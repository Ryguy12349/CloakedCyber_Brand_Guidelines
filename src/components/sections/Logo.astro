---
import { brandSettings } from '../../scripts/brandSettings.js';
import InstructionNote from '../InstructionNote.astro';



---
<section id="logo">
    <h2>Logo & Wordmark</h2>
    
    <InstructionNote>
        Upload and document your logo variations here. Ensure you provide versions for light and dark backgrounds, as well as monochrome options for print or restricted color environments.
    </InstructionNote>

    <!-- description: "Use this version in thinner spaces with a white or light backgrounds.", -->
    <!-- description: "Use this version in thinner spaces with a dark or brand-colored backgrounds.", -->
    <!-- description: "For thinner spaces with a black and white print or single-color applications.", -->
    <!-- "For thinner spaces with a dark backgrounds where color is not permitted.", -->
    <ul class="body-list">
        <li><strong>Light:</strong> Use this version with a white or light backgrounds.</li>
        <li><strong>Dark:</strong> Use this version with a dark or brand-colored backgrounds.</li>
        <li><strong>Black:</strong> Use this version with a black and white print or single-color applications.</li>
        <li><strong>White:</strong> Use this version with a dark backgrounds where color is not permitted.</li>
    </ul>


    <h3>Textless Logo</h3>
    <p>
        The textless logo is a version of the logo that does not include any text or company name.
        It should be used in contexts where the brand is already well-known, or where space is limited, such as on social media profile pictures, favicons, or small merchandise items.
        Ensure that the textless logo maintains its integrity and is easily recognizable even without the accompanying text.
    </p>
    <div class="logo-grid">
        {brandSettings.logo.variations
            .filter(variation => variation.size === "logo")
                .map(variation => (
                    <div class="logo-card">
                        <div class="logo-preview" style={`background-color: ${variation.bg}; color: ${variation.darkText ? '#000' : '#fff'}`}>
                            {variation.src ? (
                                <img src={variation.src} alt={`${brandSettings.logo.alt} - ${variation.name}`} />
                            ) : (
                                <div class="placeholder-text">[Logo: {variation.name}]</div>
                            )}
                        </div>
                        <div class="logo-info">
                            <h4>{variation.name}</h4>
                        </div>
                    </div>
                ))}
    </div>

    <h3>Wordmark</h3>
    <p>
        The wordmark is a version of the logo that includes the company name in a stylized font.
        It should be used in contexts where brand recognition is important, such as on marketing materials, websites, and social media profiles.
        Ensure that the wordmark is legible and maintains its integrity across different sizes and backgrounds.
    </p>
    <div class="logo-grid">
        {brandSettings.logo.variations
            .filter(variation => variation.size === "wordmark")
                .map(variation => (
                    <div class="logo-card">
                        <div class="logo-preview" style={`background-color: ${variation.bg}; color: ${variation.darkText ? '#000' : '#fff'}`}>
                            {variation.src ? (
                                <img src={variation.src} alt={`${brandSettings.logo.alt} - ${variation.name}`} />
                            ) : (
                                <div class="placeholder-text">[Logo: {variation.name}]</div>
                            )}
                        </div>
                        <div class="logo-info">
                            <h4>{variation.name}</h4>
                        </div>
                    </div>
                ))}
    </div>
</section>

<style>
    .logo-grid {
        --logo-card-width: 300px;
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(var(--logo-card-width), 1fr));
        gap: 2rem;
        margin-top: 2rem;
    }

    @media(max-width: 370px) {
        .logo-grid {
            --logo-card-width: 200px;
        }
    }


    .logo-card {
        border: 1px solid var(--border-color);
        border-radius: 8px;
        overflow: hidden;
        background: var(--container-bg);
    }
    .logo-preview {
        padding: 0 1.5rem;
        display: flex;
        align-items: center;
        justify-content: center;
        min-height: 200px;
        border-bottom: 1px solid var(--border-light);
    }
    .logo-preview img {
        max-width: 100%;
        height: auto;
    }
    .logo-info {
        padding: 1rem;
    }
    .logo-info h4 {
        margin: 0 0 0.5rem 0;
        font-size: 1rem;
        color: var(--heading-color);
    }
    .logo-info p {
        margin: 0;
        font-size: 0.9rem;
        color: var(--muted-text);
    }
    .placeholder-text {
        opacity: 0.5;
        font-style: italic;
        text-align: center;
    }
</style>